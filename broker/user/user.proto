syntax = "proto3";

package broker;

// user.user.add: when create user
// user.user.change: when update user

message User {
  string id = 1;
  string email = 2;
  string full_name = 3;
  string avatar_url = 4;
  bool   is_active = 5;
}
